[package]
name = "orsx-macros"
version = "0.1.1"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Proc macros for orsx (rewrite in progress)"

[lib]
proc-macro = true

[dependencies]
syn = { workspace = true, features = ["full"] }
quote = { workspace = true }
proc-macro2 = { workspace = true }

